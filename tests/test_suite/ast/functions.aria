module functions;

fn foo() -> void {}

fn add(lhs: int, rhs: int) -> int { return lhs + rhs; }
fn sub(lhs: int, rhs: int) -> int { return lhs - rhs; }

fn recursive(x: int) -> int { return recursive(x); }

fn main() -> void {
    foo();
    let x: int = sub(add(5, 6), 6);   
    recursive(66);
}