module functions;

let result: int = 0; // Expected to be 24

fn external_function(a: int, b: int, c: int) -> void;

fn add(lhs: int, rhs: int) -> int {
    return lhs + rhs;
}

fn sub(lhs: int, rhs: int) -> int {
    return lhs - rhs;
}

let otherResult: int = 0; // Expected to be 26

fn main() -> void {
    result = add(5, 9);
    result = result + add(sub(6, 3), 7);
    otherResult = result + 2;

    external_function(5, 9 * 7 + 3, (5 + 5) * 5);
}