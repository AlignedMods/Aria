module recursion;

fn fib(a: int) -> int {
    if a <= 1 { return a; }
    return fib(a - 1) + fib(a - 2);
}

fn factorial(i: long) -> long {
   if i <= 1 { return 1; }
   return i * factorial(i - 1);
}

fn sum(i: int) -> int {
    if i == 0 { return i; }
    return i + sum(i - 1);
}