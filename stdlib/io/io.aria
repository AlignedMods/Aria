module std::io;

fn println(str: []char) -> void {
    let s: StringStream;
    defer s.free();
    s.append_string(str);
    s.append_char('\n');

    printf(c"%s", s.to_ptr());
}

fn print(str: []char) -> void {
    let s: StringStream;
    defer s.free();
    s.append_string(str);

    printf(c"%s", s.to_ptr());
}

#private

extern fn printf(s: *char, ...) -> int;
