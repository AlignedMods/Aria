module std::io;

import std::core;

fn println(str: char[]) -> void {
    let s: StringStream;
    s.append(str);
    s.append('\n');

    printf(c"%.*s", s.size, s.data);
}

fn print(str: char[]) -> void {
    let s: StringStream;
    s.append(str);

    printf(s.to_ptr());
}

@private

extern fn printf(s: const char*, ...) -> int;