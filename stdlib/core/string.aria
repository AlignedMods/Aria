module std::core;

struct String {
    size: ulong;
    data: char*;
}

impl String {
    String(s: char[]) {
        size = s.len;

        unsafe {
            data = new char[s.len];
            memcpy(data, s.mem, s.len);
        }
    }

    ~String() {
        size = 0;
        unsafe delete data;
        data = null;
    }
}